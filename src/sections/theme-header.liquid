<!-- section/theme-header.liquid -->

{%- comment -%}
  This is a required section for the Shopify Theme Store.
{%- endcomment -%}

{% assign logo = section.settings.logo %}
{% assign logo_width = section.settings.logo_max_width | append: 'x' %}
{% assign h1 = false %}
{% if template == 'index' or template == 'password' %}
  {% assign h1 = true %}
{% endif %}

<section data-section-id="{{ section.id }}" data-section-type="header">

  {%- comment -%}
    Announcement bar

    Theme Store required settings
    - Show announcement
    - Text: message to announce
    - Link: link of the announcement bar

    Theme Store optional settings
    - Home page only: only shows on the home page
  {%- endcomment -%}
  {% if section.settings.announcement_bar_enabled %}
    <div class="container l center">
      {%- if section.settings.announcement_bar_home_page_only == false or template.name == 'index' -%}
        {%- if section.settings.announcement_bar_link != blank -%}
          <a href="{{ section.settings.announcement_bar_link }}">
            {{ section.settings.announcement_bar_text | escape }}
          </a>
        {%- else -%}
          <p>{{ section.settings.announcement_bar_text | escape }}</p>
        {%- endif -%}
        <hr>
      {%- endif -%}
    </div>
  {% endif %}

  <header role="banner" class="container l margin-top-2x margin-bottom-2x">

    {%- comment -%}
      Header logo
    {%- endcomment -%}
    {% if h1 %}
      <h1 class="logo">
    {% else %}
      <div class="h1 logo">
    {% endif %}
      {% if logo != blank %}
        <a href="/" title="{{ shop.name }}" class="logo-image">
          <img src="{{ section.settings.logo | img_url: logo_width }}"
               srcset="{{ section.settings.logo | img_url: logo_width }} 1x,
                       {{ section.settings.logo | img_url: logo_width, scale: 2 }} 2x"
               alt="{{ section.settings.logo.alt | default: shop.name }}">
        </a>
      {% else %}
        <a href="/" title="{{ shop.name }}">{{ shop.name }}</a>
      {% endif %}
    {% if h1 %}
      </h1>
    {% else %}
      </div>
    {% endif %}

    {%- comment -%}
      Header cart link and item count
      This href is all on one line to avoid whitespace issues

      The #MiniCart item loop is a simple demo of a non-Ajax cart summary
    {%- endcomment -%}
    <div id="MiniCart">
      <a href="/cart" id="CartLink">{{ 'cart.title' | t }} ({{ cart.item_count }} {{ 'cart.item_count' | t: count: cart.item_count }})</a>
      <div>
        {% for item in cart.items %}
          <div class="clearfix">
            <a href="{{ item.url }}">
              {% if item.image != blank %}
                <img src="{{ item.image | img_url: '50x80' }}"
                     srcset="{{ item.image | img_url: '50x80' }} 1x,
                             {{ item.image | img_url: '50x80', scale: 2 }} 2x"
                     alt="{{ item.title }}">
              {% endif %}
              {{ item.product.title }}
              <br>
              {{ item.variant.title }}
            </a>
            <br>
            <small>{{ item.price | money_with_currency }}</small>
          </div>
          <hr class="margin-top-1x margin-bottom-1x">
        {% endfor %}
        <a href="/cart">{{ 'cart.view_cart' | t }}</a>
      </div>
    </div>

    <br>

    {%- comment -%}
      Header customer account links
    {%- endcomment -%}
    {% if shop.customer_accounts_enabled %}
      <ul class="inline margin-bottom-2x">
        {% if customer %}
          <li>
            {%- if customer.first_name != blank %}
              {% capture first_name %}<a href="/account">{{ customer.first_name }}</a>{% endcapture %}
              {{ 'customer.account.logged_in_as_html' | t: first_name: first_name }}
            {% else %}
              <a href="/account">{{ 'layout.customer.account' | t }}</a>
            {% endif -%}
          </li>
          <li>{{ 'customer.account.logout' | t | customer_logout_link }}</li>
        {% else %}
          <li>{{ 'customer.login.title' | t | customer_login_link }}</li>
          <li>{{ 'customer.register.title' | t | customer_register_link }}</li>
        {% endif %}
      </ul>
    {% endif %}

    <br>

    {%- comment -%}
      Header search form
    {%- endcomment -%}
    <form action="/search" method="get" role="search">
      <label for="Search" class="visually-hidden">
        {{ 'search.title' | t }}
      </label>

      <input type="search"
        name="q"
        id="Search"
        value="{{ search.terms | escape }}"
        placeholder="{{ 'search.title' | t }}">

      <button type="submit">
        {% comment %}{% include 'icon-search' %}{% endcomment %} <!-- TO DO -->
        <span class="icon-fallback-text">{{ 'search.submit' | t }}</span>
      </button>
    </form>

  </header>

  {%- comment -%}
    Main navigation menu
  {%- endcomment -%}
  <nav role="navigation" class="container l margin-bottom-2x">
    <ul class="inline padding-bottom-2x">
      {% for link in linklists[section.settings.menu].links %}
        {% if link.links != blank %}
          <li>
            <a href="{{ link.url }}">
              {{ link.title }}
              {% comment %}{% include 'icon-arrow-down' %}{% endcomment %} <!-- TO DO -->
            </a>
            <ul>
              {% for childlink in link.links %}
                <li>
                  <a href="{{ childlink.url }}">
                    {{ childlink.title }}
                  </a>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% else %}
          <li>
            <a href="{{ link.url }}">
              {{ link.title }}
            </a>
          </li>
        {% endif %}
      {% endfor %}
    </ul>

    <hr>

  </nav>

</section>

{%- comment -%}
  Additional section styles
{%- endcomment -%}
{% if section.settings.logo != blank %}
  <style>
    .logo-image {
      display: block;
      max-width: {{ section.settings.logo_max_width }}px;
    }
  </style>
{% endif %}

{% schema %}
  {
    "name": "Header",
    "settings": [
      {
        "type": "header",
        "content": "Announcement bar"
      },
      {
        "type": "checkbox",
        "id": "announcement_bar_enabled",
        "label": "Show announcement",
        "default": false
      },
      {
        "type": "checkbox",
        "id": "announcement_bar_home_page_only",
        "label": "Show on home page only",
        "default": true
      },
      {
        "type": "text",
        "id": "announcement_bar_text",
        "label": "Announcement text",
        "default": "Announce something here"
      },
      {
        "type": "url",
        "id": "announcement_bar_link",
        "label": "Announcement link",
        "info": "Optional"
      },
      {
        "type": "header",
        "content": "Header"
      },
      {
        "type": "image_picker",
        "id": "logo",
        "label": "Logo image"
      },
      {
        "type": "range",
        "id": "logo_max_width",
        "min": 50,
        "max": 450,
        "step": 10,
        "unit": "px",
        "label": "Custom logo width",
        "default": 250
      },
      {
        "type": "link_list",
        "id": "menu",
        "label": "Menu",
        "default": "main-menu"
      }
    ]
  }
{% endschema %}
